bundle:
  name: nfcorpus-finetuning

targets:
  # Dev/default deployment settings
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7174671745009808.8.azuredatabricks.net
      root_path: ~/.bundle/${bundle.name}
